<p style="color: green"><%= notice %></p>

<h1>Friends</h1>

<div id="friends">
  <% @friends.each do |friend| %>
    <%= link_to render(friend), friend %>
  <% end %>
</div>

<%= link_to "New friend", new_friend_path %>
